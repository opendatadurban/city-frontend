{% from "../../macros/attributes.njk" import cityAttributes -%}

<div {%- if params.id %} id="{{ params.id }}"{% endif %} class="city-inset-text {%- if params.classes %} {{ params.classes }}{% endif %}"
  {{- cityAttributes(params.attributes) }}>
  {{ caller() if caller else (params.html | safe | trim | indent(2) if params.html else params.text) }}
</div>
