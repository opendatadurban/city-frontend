{% from "../../macros/attributes.njk" import cityAttributes %}
{% from "../button/macro.njk" import cityButton -%}

<div class="city-cookie-banner {%- if params.classes %} {{ params.classes }}{% endif %}" data-nosnippet role="region" aria-label="{{ params.ariaLabel | default("Cookie banner", true) }}"
  {%- if params.hidden %} hidden{% endif %}
  {{- cityAttributes(params.attributes) }}>
  {% for message in params.messages %}
  <div class="city-cookie-banner__message {%- if message.classes %} {{ message.classes }}{% endif %} city-width-container" {%- if message.role %} role="{{ message.role }}"{% endif %}
    {{- cityAttributes(message.attributes) -}}
    {%- if message.hidden %} hidden{% endif %}>

    <div class="city-grid-row">
      <div class="city-grid-column-two-thirds">
        {% if message.headingHtml or message.headingText %}
        <h2 class="city-cookie-banner__heading city-heading-m">
          {{ message.headingHtml | safe | trim | indent(10) if message.headingHtml else message.headingText }}
        </h2>
        {% endif %}
        <div class="city-cookie-banner__content">
          {% if message.html %}
          {{ message.html | safe | trim | indent(10) }}
          {% elif message.text %}
          <p class="city-body">{{ message.text }}</p>
          {% endif %}
        </div>
      </div>
    </div>

    {% if message.actions %}
    <div class="city-button-group">
    {% for action in message.actions %}
      {% set buttonHtml -%}
        {% if not action.href or action.type == "button" %}
          {{ cityButton({
            "text": action.text,
            "type": action.type if action.type else "button",
            "name": action.name,
            "value": action.value,
            "classes": action.classes,
            "href": action.href,
            "attributes": action.attributes
          }) }}
        {% else %}
          <a class="city-link {%- if action.classes %} {{ action.classes }}{% endif %}" href="{{ action.href }}"
            {{- cityAttributes(action.attributes) }}>
            {{- action.text -}}
          </a>
        {% endif %}
      {%- endset %}
      {{ buttonHtml | safe | trim | indent(6) }}
    {% endfor %}
    </div>
    {% endif %}

  </div>
  {% endfor %}
</div>
