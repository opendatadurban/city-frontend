{% extends "layouts/examples.njk" %}

{% from "city/components/back-link/macro.njk" import cityBackLink %}
{% from "city/components/button/macro.njk" import cityButton %}
{% from "city/components/radios/macro.njk" import cityRadios %}

{% block beforeContent %}
  {{ cityBackLink({
    href: "/"
  }) }}
{% endblock %}

{% block content %}
<h1 class="city-heading-xl">Multiple radio groups</h1>

<div class="city-grid-row">
  <div class="city-grid-column-one-half">
    <form method="get" novalidate>
      <fieldset class="city-fieldset">
        <legend class="city-fieldset__legend city-fieldset__legend--l">
          <h2 class="city-fieldset__heading">
            What is your favourite colour?
          </h2>
        </legend>
        <h3 class="city-heading-m">Warm colours</h3>
        {{ cityRadios({
          idPrefix: "warm",
          name: "colour",
          items: [
            {
              value: "red",
              text: "Red",
              conditional: {
                html: '<p class="city-body">Nice choice, I like red too.</p>'
              }
            },
            {
              value: "orange",
              text: "Orange",
              conditional: {
                html: '<p class="city-body">Nice choice, I like orange too.</p>'
              }
            }
          ]
        }) }}

        <h3 class="city-heading-m">Cool colours</h3>
        {{ cityRadios({
          idPrefix: "cool",
          name: "colour",
          items: [
            {
              value: "blue",
              text: "Blue",
              conditional: {
                html: '<p class="city-body">Nice choice, I like blue too.</p>'
              }
            },
            {
              value: "green",
              text: "Green",
              conditional: {
                html: '<p class="city-body">Boo, you monster...</p>'
              }
            }
          ]
        }) }}

        <h3 class="city-heading-m">Do you like colours in general?</h3>
        {{ cityRadios({
          idPrefix: "colours-in-general",
          name: "colours-in-general",
          items: [
            {
              value: "yes",
              text: "Yes",
              conditional: {
                html: '<p class="city-body">Fair enough.</p>'
              }
            },
            {
              value: "no",
              text: "No",
              conditional: {
                html: '<p class="city-body">No judgment.</p>'
              }
            }
          ]
        }) }}
      </fieldset>
      {{ cityButton({
        text: "Submit"
      }) }}
    </form>
  </div>
  <div class="city-grid-column-one-half">
    <form method="get" novalidate>
      <fieldset class="city-fieldset">
        <legend class="city-fieldset__legend city-fieldset__legend--l">
          <h2 class="city-fieldset__heading">
            What is your least favourite colour?
          </h2>
        </legend>
        <h3 class="city-heading-m">Warm colours</h3>
        {{ cityRadios({
          idPrefix: "least-warm",
          name: "colour",
          items: [
            {
              value: "red",
              text: "Red",
              conditional: {
                html: '<p class="city-body">Nice choice, I like red too.</p>'
              }
            },
            {
              value: "orange",
              text: "Orange",
              conditional: {
                html: '<p class="city-body">Nice choice, I like orange too.</p>'
              }
            }
          ]
        }) }}

        <h3 class="city-heading-m">Cool colours</h3>
        {{ cityRadios({
          idPrefix: "least-cool",
          name: "colour",
          items: [
            {
              value: "blue",
              text: "Blue",
              conditional: {
                html: '<p class="city-body">Nice choice, I like blue too.</p>'
              }
            },
            {
              value: "green",
              text: "Green",
              conditional: {
                html: '<p class="city-body">Boo, you monster...</p>'
              }
            }
          ]
        }) }}
      </fieldset>
      {{ cityButton({
        text: "Submit"
      }) }}
    </form>

    <h3 class="city-heading-m">A question that is not in a form</h3>
    {{ cityRadios({
      idPrefix: "question-not-in-form",
      name: "question-not-in-form",
      items: [
        {
          value: "yes",
          text: "Yes",
          conditional: {
            html: '<p class="city-body">Fair enough.</p>'
          }
        },
        {
          value: "no",
          text: "No",
          conditional: {
            html: '<p class="city-body">No judgment.</p>'
          }
        }
      ]
    }) }}
  </div>
</div>
{% endblock %}
